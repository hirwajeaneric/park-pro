export type SignInFormTypes = {
    email: string;
    password: string;
}

export type SignUpFormTypes = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
}

export type signInResponseType = {
    token: string;
}