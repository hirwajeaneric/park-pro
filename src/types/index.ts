export type SignInFormTypes = {
    email: string;
    password: string;
}

export type SignUpFormTypes = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
}

export type UserProfileType = {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    role: string;
    parkId: string;
  }